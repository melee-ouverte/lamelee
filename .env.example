# Database
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://user:password@localhost:5432/lamelee?schema=public"

# NextAuth.js Configuration
# The base URL of your application
# In production: https://yourdomain.com
# In development: http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# NextAuth.js Secret
# A random string used to encrypt tokens and email verification hashes
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here-generate-with-openssl-rand-base64-32"

# GitHub OAuth
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/auth/callback/github (development)
# Authorization callback URL: https://yourdomain.com/api/auth/callback/github (production)
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"
